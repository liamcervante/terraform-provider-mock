name: test on_push_tags

on:
  push:
    tags:
      - t*

permissions:
  contents: read

jobs:
  test-job:
    name: "Run test job"
    runs-on: ubuntu-latest

    steps:
      - env:
          ctx: ${{ toJSON(github) }}
        run: |
          echo ${{ github.event.push.ref }}
          echo ${{ env.ctx }}
